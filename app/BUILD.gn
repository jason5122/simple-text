source_set("app") {
  if (is_mac) {
    sources = [
      "app.cc",
      "cocoa/gl_app.mm",
      "cocoa/gl_window.mm",
      "window.cc",
    ]

    frameworks = [ "Cocoa.framework" ]
  }

  deps = [ "//base" ]
}
