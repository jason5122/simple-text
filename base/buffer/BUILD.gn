source_set("buffer") {
  sources = [
    "piece_tree.cc",
    "piece_tree_rbtree.cc",
  ]

  # configs += [ "//build/config/compiler:prevent_unsafe_narrowing" ]

  deps = [
    "aho_corasick",
    "//third_party/fmt",
  ]
}
