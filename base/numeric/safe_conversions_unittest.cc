#include "base/numeric/safe_conversions.h"
#include <gtest/gtest.h>

namespace base {

// TEST(SafeConversionsTest, CheckedCast) {
//     EXPECT_EQ(checked_cast<size_t>(uint8_t{1}), size_t{1});
//     EXPECT_EQ(checked_cast<size_t>(int{1}), size_t{1});
// }

// TEST(SafeConversionsDeathTest, FailedCast) { EXPECT_DEATH(checked_cast<int>(size_t{1}), ""); }

}  // namespace base
