import("//build/config/mac/mac_app_bundle.gni")

mac_app_bundle("mac_startup_benchmark") {
  output_name = "Mac Startup Benchmark"

  sources = [ "main.mm" ]

  frameworks = [
    "Cocoa.framework",
    "QuartzCore.framework",
  ]
  defines = [ "GL_SILENCE_DEPRECATION" ]
}
