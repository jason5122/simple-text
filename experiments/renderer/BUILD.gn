executable("renderer") {
  sources = [ "main.mm" ]

  frameworks = [
    "Cocoa.framework",
    "OpenGL.framework",
    "QuartzCore.framework",
  ]
  defines = [ "GL_SILENCE_DEPRECATION" ]

  deps = [
    "//base",
    "//gfx",
    "//gl",
    "//third_party/spdlog",
  ]
}
