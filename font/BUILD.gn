group("font") {
  if (is_mac) {
    # public_deps = [ "core_text" ]
    public_deps = [ "pango" ]
  }
  if (is_linux) {
    public_deps = [ "pango" ]
  }
  if (is_win) {
    public_deps = [ "directwrite" ]
  }
}
