source_set("gfx") {
  sources = [ "device.cc" ]

  # OpenGL.
  sources += [
    "gl/gl_device.cc",
    "gl/gl_frame.cc",
    "gl/gl_surface.cc",
  ]

  deps = [
    "//gl",
    "//third_party/spdlog",
  ]
}
