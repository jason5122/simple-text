source_set("tests") {
  testonly = true

  sources = [ "hash_map_test.cc" ]

  # configs += [ ":optimizations" ]

  deps = [ "//third_party/googletest:gtest" ]
}

config("optimizations") {
  cflags = [
    "-flto=thin",
    "-O2",
  ]
  ldflags = cflags
}
