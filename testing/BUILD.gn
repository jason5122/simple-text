executable("test_runner") {
  testonly = true

  sources = [ "empty_main.cc" ]

  deps = [ "//third_party/googletest:gtest_main" ]

  if (is_mac) {
    deps += [
      "//base/tests",
      "//font/tests",
    ]
  }
}
