executable("test_runner") {
  testonly = true

  deps = [ "//third_party/googletest:gtest_main" ]

  if (is_mac) {
    deps += [
      "//base/tests",
      "//font/tests",
      "//renderer/tests",
    ]
  }
}
