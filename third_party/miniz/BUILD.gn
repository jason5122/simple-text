assert(path_exists("src/miniz.c"),
       "Missing submodules. Run: git submodule update --init --recursive")

static_library("miniz") {
  sources = [
    "src/miniz.c",
    "src/miniz_tdef.c",
    "src/miniz_tinfl.c",
    "src/miniz_zip.c",
  ]

  configs -= [ "//build/config/compiler:simple_text_code" ]
  configs += [ "//build/config/compiler:no_simple_text_code" ]

  public_configs = [ ":miniz_includes" ]
}

config("miniz_includes") {
  include_dirs = [
    "include",
    "src",
  ]
}
