source_set("ui") {
  sources = [
    "mac/EditorView.mm",
    "mac/WindowController.mm",
    "renderer/atlas.cc",
    "renderer/atlas_renderer.cc",
    "renderer/cursor_renderer.cc",
    "renderer/renderer.cc",
  ]

  frameworks = [
    "Cocoa.framework",
    "QuartzCore.framework",
    "OpenGL.framework",
  ]

  defines = [ "GL_SILENCE_DEPRECATION" ]

  deps = [
    "//base",
    "//util",
  ]
}
