source_set("cocoa") {
  sources = [
    "OpenGLView.mm",
    "app_mac.mm",
    "app_window_mac.mm",
  ]

  frameworks = [
    "Cocoa.framework",
    "QuartzCore.framework",
    "OpenGL.framework",
  ]
  defines = [ "GL_SILENCE_DEPRECATION" ]

  public_deps = [ "//third_party/glad" ]
}
