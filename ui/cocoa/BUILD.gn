source_set("cocoa") {
  sources = [
    "EditorView.mm",
    "WindowController.mm",
  ]

  frameworks = [
    "Cocoa.framework",
    "QuartzCore.framework",
  ]

  # TODO: Move all OpenGL code to //ui/renderer.
  frameworks += [ "OpenGL.framework" ]
  defines = [ "GL_SILENCE_DEPRECATION" ]

  # Why do we need this here, even though we don't use any libepoxy headers?
  public_deps = [ "//third_party/libepoxy" ]

  deps = [
    "//base",
    "//util",
  ]
}
