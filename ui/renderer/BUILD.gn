source_set("renderer") {
  sources = [
    "atlas.cc",
    "image_renderer.cc",
    "rect_renderer.cc",
    "shader.cc",
  ]

  if (!is_win) {
    sources += [ "text_renderer.cc" ]
  }

  deps = [
    "shaders",
    "//base",
    "//font",
    "//third_party/libgrapheme",
    "//third_party/libpng",
    "//ui/icons",
    "//ui/sample_files",
    "//util",
  ]

  public_deps = [ "//third_party/glad" ]
}
