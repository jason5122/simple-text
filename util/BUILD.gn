source_set("util") {
  sources = [ "file_util.cc" ]

  if (is_mac) {
    sources += [
      "file_util_mac.mm",
      "log_util_mac.mm",
    ]

    frameworks = [ "Cocoa.framework" ]
  }

  if (is_linux) {
    sources += [ "file_util_linux.cc" ]
  }
}
